# PayPal REST API credentials (developer.paypal.com → Apps & Credentials).
# Without these the checkout button lands on /checkout/demo, which explains
# that payments aren't connected yet instead of failing quietly.
#
# This is a separate thing from a PayPal.com pay link — a pay link is one
# fixed price, and these credentials are what let the server create a
# PayPal order for each basket's own total and charge that automatically.
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=

# "sandbox" while testing with a PayPal developer sandbox account; unset (or
# "live") to take real payments through the production PayPal API.
PAYPAL_ENV=

# Resend API key (resend.com → API Keys) for order emails: a notification to
# the shop's own address, and a confirmation with the order reference to the
# buyer. Without this the order still captures fine — no emails are sent.
RESEND_API_KEY=

# Optional. Left unset, emails send from Resend's shared sandbox address,
# which needs no setup and can send to anyone. Only set this once a real
# domain has been added and verified in Resend, e.g.
# "Secret Source <orders@yourdomain.com>".
RESEND_FROM_EMAIL=

# The password for /admin — logs in to the stock-editing panel. Pick
# something only you know; there's no username, just this.
ADMIN_PASSWORD=

# A long random string used to sign the admin login session — not a
# password you type anywhere, just a secret the server uses to prove its
# own cookies weren't forged. Generate one with, e.g., `openssl rand -hex 32`.
ADMIN_SESSION_SECRET=

# A GitHub fine-grained personal access token (github.com → Settings →
# Developer settings → Fine-grained tokens), scoped to only the
# mattpop52/secret-source repo, with Contents: Read and write permission.
# Saving in the admin stock panel uses this to commit lib/stock.json
# straight to the repo, which Vercel then redeploys automatically.
GITHUB_TOKEN=
